# Project-local bashrc for silver-lamp
# Source this file in your shell to enable project-specific aliases and environment variables.

# Alias for Angular CLI (assumes local install via pnpm)
alias ng="pnpm exec ng"
alias nest="pnpm exec nest"

# Add more project-specific aliases or environment variables below
