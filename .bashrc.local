# Project-local .bashrc/.zshrc
# rename to '.bashrc' or '.zshrc'
# then run: $ source .[ba|z]shrc

# Aliases (assumes local install via pnpm)
alias turbo="pnpm exec turbo"
alias ng="pnpm exec ng"
alias nest="pnpm exec nest"
